Agents in `~/dev/advance` report lgrep semantic search timeouts even though the lgrep server is running and the repo has a populated semantic index. Evidence shows Vision proxy errors (`retry_exhausted: context deadline exceeded`) while lgrep hybrid search performs expensive LanceDB FTS/vector index creation on the query path. This makes semantic search unreliable for ADV agents and causes them to abandon the primary local-code intelligence tool.